/**
 * Auth-module-specific type helpers.
 *
 * The Express Request augmentation for `req.user` is co-located in
 * `types/express.d.ts` so it's loaded once for the whole app. This file
 * exists as a stable import path for the auth module and a place to
 * put future auth-only types.
 */

export {};
